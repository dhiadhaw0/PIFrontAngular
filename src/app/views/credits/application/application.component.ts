import { Component } from '@angular/core'
import { RouterLink } from '@angular/router'

@Component({
  selector: 'credits-application',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './application.component.html',
  styles: ``,
})
export class ApplicationComponent {} 